# Writable-file reconciliation prototype

```nix
home.file.".config/foo/config" = {
  text = ''
    theme=light
    font-size=14
  '';
  reconciliation.enable = true;
};
```

The shared file submodule also exposes this option through `xdg.configFile`
and the other XDG file sets. It accepts individual UTF-8 text files only.
`recursive` and `force` cannot be combined with reconciliation. There is no
force-resolution command in this prototype.

## Architecture

This prototype includes upstream 2c0350c759688177331b8f5242311fae8877bdb3,
matching Strata's Home Manager input before the local override. Reconciled
targets are skipped in the batched linker's classification loop.

* `modules/lib/file-type.nix` defines the generic file submodule, including
  `source`, `text`, `target`, `executable`, `recursive`, `onChange`, and `force`.
* `modules/files.nix` builds `home-files`. Reconciled sources are copied into
  this immutable output, preserving a baseline even for a symlink source.
* `home.extraBuilderCommands` installs `reconciliation.json` alongside
  `home-files` in every generation. This records ownership, not live contents.
* `modules/lib-bash/activation-init.sh` sets `oldGenPath` from the current
  generation GC root and `newGenPath` from the activation script's generation.
* `checkReconciledFiles` creates an in-memory plan before `writeBoundary` and
  before `checkFilesChanged`. Any divergence aborts without installing files.
* Existing `checkLinkTargets` and `linkGeneration` skip new reconciled targets.
  Cleanup skips old reconciled targets, including ones replaced with symlinks.
* At the start of `linkGeneration`, all live snapshots are checked again. Each
  changed result is staged in its destination directory and atomically renamed.
  `onChange` reflects the actual planned content change.

No live content is placed in the Nix store. Python becomes an activation
dependency, including for generations that relinquish reconciled files.

## Reconciliation

With old generated bytes A, live bytes B, and new generated bytes C:

| Condition | Result |
| --- | --- |
| B = A | Install C |
| C = A | Preserve B |
| B = C | Preserve B |
| Both changed, non-overlapping line edits | Install merged text |
| Overlapping edits | Fail; preserve B |

The merge uses line edit ranges against A, without producing conflict markers.
Identical edits coalesce. Adjacent replacements are accepted, so changing the
theme on one line and font size on the next merges. Inserting at the boundary
of another edit is rejected conservatively. This is a text merge, not a proof
of semantic compatibility; it does not understand settings or file formats.

## Edge cases

| Situation | Prototype behavior |
| --- | --- |
| First activation, target absent | Create owner-writable regular file, mode 0600 or 0700 |
| Foreign file already exists | Fail, even if contents equal C and backups are enabled |
| Declaration removed or disabled | Preserve live file and relinquish ownership |
| Symlink-managed to reconciled | Accept only the exact old generation's home-files link; materialize C |
| Reconciled to symlink-managed | Fail while a live target exists; manually move it aside first |
| Executable declaration changes | Fail for an existing reconciled regular file |
| Live permission bits change | Preserve them; reject a file lacking owner-write permission |
| Application deletes managed file | Fail; do not silently recreate it |
| Application replaces file with symlink, directory, or hardlink | Fail |
| Symlink in target's parent path | Fail |
| Binary or non-UTF-8 contents | Fail |
| Rollback between prototype generations | Reconcile using the currently active baseline and older desired contents |
| Rollback to a generation built before this feature | Its old activation code cannot enforce these safeguards; unsupported |

Removing and later re-adding a declaration does not recover its baseline: a
remaining live file is foreign again. Manually moving it aside is the explicit
resolution path. Backups and permanent `force` settings do not bypass conflicts.

Both standalone `home-manager switch` and the NixOS Home Manager module execute
the same activation package and use the current-generation GC root. This check
therefore applies in both modes. It does not make an entire NixOS system switch
transactional, nor undo profile changes already performed by an external driver.

## Safety limits

Applications must be quiescent during activation. Snapshots detect changes
between planning and installation, with a final per-file check, but POSIX rename
does not provide compare-and-swap against concurrent application writes. The
prototype is not a filesystem transaction: I/O failure or concurrent changes
after installation starts can leave earlier results installed. Ordinary detected
merge conflicts occur before the write boundary and install nothing. Existing
activation initialization and user-defined activation hooks retain their own
behavior.

Unix permission bits are preserved on replacement. Existing regular files with
different ownership from the activating user/group, special permission bits,
ACLs, or extended attributes are rejected rather than losing that metadata.

The manifest and separate reconciliation helper allow additional strategies
later without changing normal file generation. Structured merges, managed
fragments, recursive directories, and automatic Nix updates are out of scope.

## Tests

```sh
PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tests/modules/files -p '*_test.py' -v
nix-build tests/modules/files/reconciliation-integration.nix --no-out-link
```

The integration derivation is also registered as `files-reconciliation` in
`tests/integration/default.nix`. It runs the generated file activation scripts
against temporary homes, including default symlinks, migration, hooks, rollback,
foreign collisions, and pre-boundary conflict failure. It does not run a NixOS
VM or alter the real user's profile or home files.
